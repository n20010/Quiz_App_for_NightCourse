<% provide(:title, "Linuxanswer<#{params[:id]}>") %>
<div class="center jumbotron">
  <h2>Answer</h2>
  <h1><%= @ques.answer %></h1>
  <%= link_to("I will Never Give Up", "/linux/#{params[:id]}/question", class: "btn btn-lg btn-primary")%>
</div>